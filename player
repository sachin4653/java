package com.vivi;

public class player {
    int number = 0;
    public void guess() {
        number = (int) (Math.random() * 10);
        System.out.println("guessed the number : " + number );

    }
}
